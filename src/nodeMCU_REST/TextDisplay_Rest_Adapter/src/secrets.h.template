/**
 * secrets.h.template
 * 
 * Template for secrets.h - Copy this file to secrets.h and fill in your credentials
 * 
 * IMPORTANT: secrets.h is git-ignored and should NEVER be committed to version control
 * 
 * Setup instructions:
 *   1. Copy this file: cp src/secrets.h.template src/secrets.h
 *   2. Edit src/secrets.h with your actual credentials
 *   3. Never commit secrets.h (it's already in .gitignore)
 */

#ifndef SECRETS_H
#define SECRETS_H

// =====================================================
// WiFi Credentials
// =====================================================
// Replace with your WiFi network name (SSID)
#define SECRET_SSID "your-wifi-ssid"

// Replace with your WiFi password
#define SECRET_KEY "your-wifi-password"

// =====================================================
// Oracle ORDS Database Credentials (if using database logging)
// =====================================================
// Replace with your Oracle ORDS password for database logging
#define ORACLE_ORDS_PASS "your-oracle-password"

// =====================================================
// OAuth Credentials (if using OAuth authentication)
// =====================================================
// Replace with your OAuth client ID
#define OAUTH_CLIENT_ID_SECRET "your-oauth-client-id"

// Replace with your OAuth client secret
#define OAUTH_CLIENT_SECRET_SECRET "your-oauth-client-secret"

#endif // SECRETS_H
